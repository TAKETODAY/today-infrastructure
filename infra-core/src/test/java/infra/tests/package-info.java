/**
 * Shared utilities that are used internally throughout the test suite but are not
 * published. This package should not be confused with {@code test}
 * which contains published code from the 'infra-test' module.
 */
package infra.tests;
