/**
 * Query model, example-based queries, query factories and condition strategies.
 *
 * <p>This package couples the declarative query contracts ({@code QueryStatement},
 * {@code QueryCondition}) with their built-in implementations, the example-based
 * query ({@code ExampleQuery}) and the condition model ({@code Condition},
 * {@code ConditionTree}) together with the {@code PropertyConditionStrategy}
 * implementations that build predicates from annotations.
 *
 * @since 5.0
 */
@NullMarked
package infra.persistence.query;

import org.jspecify.annotations.NullMarked;
