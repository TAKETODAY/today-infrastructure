description = 'Starter for using JDBC with the HikariCP connection pool'

dependencies {
  api 'com.zaxxer:HikariCP'
  api project(':infra-jdbc')
  api project(':infra-starter')
  api(project(":module:infra-app-jdbc"))

}

