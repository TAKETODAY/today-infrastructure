description = "Starter using blocking HTTP clients (RestClient, RestTemplate and HTTP Service Clients)"

dependencies {
	api(project(":infra-starter"))
	api(project(":infra-starter:infra-starter-jackson"))

	api(project(":module:infra-app-restclient"))
}
