description = "Starter using reactive HTTP clients (WebClient and HTTP Service Clients)"

dependencies {
	api(project(":infra-starter"))
	api(project(":infra-starter:infra-starter-jackson"))

	api(project(":infra-web-reactive"))

	api("io.projectreactor.netty:reactor-netty-http")
}
